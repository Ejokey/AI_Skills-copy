# Лог решений

Датированные решения и изменения, которые влияют на дальнейшую работу.
Новые записи — сверху.

Формат: дата → что решили → почему (если неочевидно).

---

## 2026-08-10 — Стадии 0–2 переведены на haiku (замер, обратимо)

`tender-intake`, `tender-analyst`, `tender-estimate` получили `model: haiku` во frontmatter.
Раскладка и переключение — `scripts/agent-model.sh`, лестница отката — `.claude/agents/MODELS.md`.
`tender-deck`, `tender-reviewer`, `tender-docs` не трогали: там цена ошибки высокая.

→ Почему: приёмка и разбор файлов не требуют сильной модели, а стоили столько же,
сколько сборка КП. Это замер, а не окончательное решение — оценивается на ближайших
тендерах, откат в одну команду.

Сторонние модели (deepseek, gpt-5.6-luna через opencode-go) в агенты Claude Code
подключить нельзя — поле `model` принимает только модели Anthropic. Для них — `hermes`.

## 2026-08-06 — Выделен тендерный пайплайн в `tenders/`

Тендерная работа вынесена из `projects/` в отдельную папку `tenders/` со стадиями
`00-source / 01-analysis / 02-estimate / 03-proposal / 04-application` и файлом состояния
`STATUS.md` на каждый тендер. Процесс — `tenders/PROCESS.md`. Добавлены агенты
`tender-intake` (приёмка) и `tender-analyst` (анализ участия); `tender-docs` перенацелен
со стадии анализа на сборку документов заявки.

→ Почему: структура папок теперь повторяет процесс, а `STATUS.md` держит состояние вне
переписки — субагент стартует без контекста, и возврат к любой стадии не требует
пересказывать историю.

Старые папки тендеров в `projects/` не мигрированы — переносятся по мере возвращения к ним.

## 2026-08-06 — Развёрнута агентная система

Введено разделение: `memory/` (факты, обновляются в процессе) и `skills/` (методология,
меняется осознанно). `AGENTS.md` — точка входа для любого ИИ, `CLAUDE.md` импортирует его.

→ Почему: факты (контакты, цены, правила) были размазаны по `SKILL.md` и справочнику
проекта, дублировались и расходились. Теперь при конфликте прав `memory/`.

## 2026-08-06 — Файлы из Telegram разложены по проектам

Созданы папки: ГРК, Отто Бокк, Сапфир, Приморский завод, `_Компания`,
«Презентации по тендерам». Скриншоты портфолио разложены по компаниям.

## 2026-08-06 — `АО МЕДИЦИНА.zip` исключён из git

147 МБ, лимит GitHub — 100 МБ. Файл остаётся локально, в репозиторий не попадает.
