node_modules/
.freebuff/

# Тендерные исходники — восстановимы у заказчика, не версионируем.
# Состав фиксируется таблицей "Опись" в STATUS.md каждого тендера.
tenders/*/00-source/

# Exceeds GitHub's 100MB file size limit — kept locally only
projects/АО Медицина 2/АО МЕДИЦИНА.zip

# Скретч-каталоги сессии: черновые скрипты и распакованный текст исходников
# тендеров (те же данные, что tenders/*/00-source/ намеренно не версионирует)
_deckbuild/
scratch_voksis/
