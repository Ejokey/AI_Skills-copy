# Справочник по проекту: подготовка тендерных КП и презентаций

Этот документ — накопленная база знаний по работе над коммерческими предложениями (КП) и презентациями для тендеров. Предназначен для использования любыми ИИ-агентами, продолжающими эту работу.

---

## 1. Контекст и структура компании

**Основная организация:** ООО «Агентство «Социальные Сети»» (SNAgency, snetwork.ru)
**Связанная организация:** AIR / ООО «КСИИ» (airelations.io) — используется как отдельное юрлицо для части тендеров, но фактически работает как одна компания с общим портфолио и общим руководителем по коммерческим вопросам.

**Профиль работы:** подготовка тендерных откликов и коммерческих предложений для проектов веб-разработки, digital-маркетинга и внедрения ИИ-решений. Итоговый результат — точные, вылизанные PDF/PPTX презентации, сдаваемые в срок.

### Контактные лица

| Роль | Данные |
|---|---|
| Владислав (контактное лицо в КП SNAgency и AIR) | info@snetwork.ru / info@airelations.io, +7 (495) 247-51-55 / +7 (495) 247-50-54 |
| Владислав Лицин | указан как контактное лицо в предложениях AIR |
| Екатерина Миличихина (коллега) | ekaterina.milichikhina@snetwork.ru — фигурирует в части КП SNAgency |
| Руководитель / руководство | даёт все ключевые коммерческие вводные — цены, сроки, финальные условия. **Все такие решения нужно уточнять у руководителя, а не принимать самостоятельно.** |

### Референсные кейсы в портфолио
Интер РАО, Росатом/Атоммедиа, КСК (Калужская энергетическая компания), OVAL, АГК, компания в сфере недвижимости (NDA), РМОУ, ВЦИОМ.

---

## 2. Активные / завершённые проекты

- **АО «Медицина»** (SNAgency) — КП на 14 слайдов, HTML→PDF pipeline. Открытые вопросы: какой слайд заменить, нужен ли экспорт в PPTX.
- **«Напитки Вместе» / АБ ИнБев Эфес** (AIR Relations) — КП на 25 слайдов. Оставалось добавить цены на хостинг/лицензии на слайд 12 и колл-аут про независимое внедрение ИИ-ассистента от основного вендора сайта.
- **«Добрые Дела»** (SNAgency) — КП на 15 слайдов, готово и сдано. Контакт: Екатерина Миличихина.
- **Воксис** (SNAgency) — КП на 19 слайдов + формальная тендерная заявка (Приложение №1), готово. Подписант: Терехов Д.Э., Генеральный директор.
- **ГРК — карьерный сайт** (AIR) — тендер b2b-center № 4543735, срок подачи 06.08.2026 23:59. Смета собирается под буквальную форму заказчика (`Форма_ТКП_обязательно_заполнить_.xlsx`, лист «Приложение №1.1. к ТКП» — 8 этапов, п. 10.1 ТЗ). Цифр от заказчика не было — оценка агентства по запросу, без реального руководителя-согласования. Текстовая структура (16 слайдов) согласована в 2 редакции, отправлена в Google Drive (папка «ГРК») как Google Doc для комментариев. HTML→PDF v1 собран (`projects/ГРК/deck/`, генератор на Node+Playwright — Python в этом окружении недоступен, см. ниже) — 16 слайдов, `projects/ГРК/КП_ГРК_карьерный_сайт_v1.pdf`. Формальная форма ТКП (xlsx) ещё не заполнена нашими данными.

---

## 3. Правила содержания слайдов (обязательно соблюдать)

- Переносы строк — только на границе предложения, не посреди фразы.
- Избегать формулировок вроде «целевая для вас CMS» и явной привязки текста слайдов к критериям тендера.
- Формулировки про санкции/штрафы — минимальные и без жирного выделения.
- Названия NDA-клиентов — держать в абстрактном виде (без прямого указания).
- Финансовые цифры менять **только по явному указанию** — никогда самостоятельно.
- Если пользователь явно просит прикинуть цену/сроки самостоятельно (нет реальных данных от руководителя): считать через внутреннюю часовую ставку (обсуждалась ставка 3 500 ₽/час, актуальность проверять), но **саму ставку и итоговое количество часов по проекту нигде не публиковать** — ни в колоде, ни в официальной форме ТКП/сметы за пределами построчных данных, которые буквально требует форма заказчика. Публикуются только суммы в рублях и сроки по этапам.
- Стратегические добавления (новый скоуп, рекомендации) — **только после явного одобрения**, не вставлять по своей инициативе.
- Принцип минимального вмешательства: редактировать только то, что явно запрошено; нетронутые слайды должны оставаться байт-в-байт идентичными.

---

## 4. Технический pipeline

### PDF-пайплайн (HTML → PDF)
- Стек: Python `build.py` (генератор HTML-слайдов) → Playwright headless Chromium (1280×720px, `print_background=True`) → `pypdf` / `fitz` (PyMuPDF) для вставки ссылок и работы со страницами.
- **В этом окружении (Windows, локальный Claude Code) Python недоступен.** Рабочая замена: Node.js `build.js` генерирует `deck.html` (все слайды в одном файле, картинки — инлайн base64 через `img.json`, `page-break-after: always` между слайдами) → `npm install playwright` + `npx playwright install chromium` → `render.js` открывает `deck.html` и вызывает `page.pdf({width:'1280px', height:'720px', printBackground:true})` — получает многостраничный PDF за один проход, без склейки через fitz. См. `projects/ГРК/deck/` как рабочий пример.
- В том же `render.js` встроена программная проверка переполнения (сравнение `getBoundingClientRect().bottom` каждого элемента с верхним краем `.footer`) и полистовые PNG-скриншоты (`.slide` → `elementHandle.screenshot()`) — заменяют `pdftoppm`/`tesseract` из Linux-версии пайплайна для визуальной QA без дополнительных инструментов.
- Все ассеты — base64 в `img.json` для самодостаточного рендеринга HTML. После добавления новых картинок — перекодировать и перезагрузить.
- Проверка переполнения (overflow) должна мериться **от верхнего края футера**, а не от низа слайда.
- При изменении количества слайдов синхронно обновлять три вещи: `footer(n, total=N)`, номера страниц в оглавлении (TOC), и `page_ranges='1-N'` в `render.py`.
- DPI-рассинхрон логотипов даёт «сплющивание» — чинится обрезкой пустых полей и заданием `height: Xpx; width: auto`.
- `object-position: top` — сохраняет шапку при cover-обрезке высоких скриншотов.

### PDF-редактирование (PyMuPDF/fitz)
- Некоторые `.docx`/`.pdf` на деле — ZIP-архивы с нумерованными `.txt`/`.jpeg` файлами внутри → использовать `zipfile.ZipFile`, а не `pypdf`/`python-docx`.
- Кириллические имена файлов ломают некоторые Python-библиотеки → копировать в `/tmp/w/` с ASCII-именами перед обработкой.
- Декоративные «блобы»/подложки нужно извлекать через `fitz.Pixmap` + SMask для корректного RGBA-альфа-канала (`extract_image()` теряет прозрачность).
- Именованные PDF-ссылки (named destinations) теряются при `insert_pdf()` → пересобирать вручную через `insert_link({'kind': fitz.LINK_GOTO, ...})`.
- Базовые шрифты (helv, hebo) не умеют в кириллицу → использовать Liberation Sans TTF (`/usr/share/fonts/truetype/liberation/`).
- Замена текста в футере требует `fitz.Font(fontfile=TTF_PATH)` с `.text_length()` для корректного измерения кириллицы.
- Надёжная замена страницы — через `show_pdf_page()` + `update_stream(x, b" ")`; избегать `delete_page`/`insert_page` (сдвигают индексы).

### PPTX
- `pptxgenjs` (Node.js), масштаб px→inches: `S = 13.333/1280`.
- Внутренние ссылки между слайдами — через `{slide: N}`.

### Прочие инструменты
- QA/проверка: `pdftoppm` для извлечения слайдов, `tesseract` для OCR-верификации, когда просмотрщик изображений не справляется.
- Excel: `openpyxl` с повторным объединением merged-ячеек после записи; `recalc.py` (`/mnt/skills/public/xlsx/scripts/recalc.py`) для пересчёта формул.
- Шрифты: Manrope (кириллица, бренд Интер РАО), Liberation Sans (общая кириллица в PDF).
- Все финальные файлы — в `/mnt/user-data/outputs/`.

---

## 5. Подход к работе (для агентов)

1. **Итеративная валидация по скриншотам** — референсные скриншоты задают пиксель-точный стандарт; перед сдачей — программная проверка overflow/выравнивания.
2. **Минимальное вмешательство** — менять только явно запрошенное; о стратегических решениях — сообщать, а не решать самому.
3. **Двухбрендовая структура** — КП подаются либо от SNAgency, либо от AIR, в зависимости от тендера; портфолио и руководитель по коммерции — общие.
4. **Согласование текста перед рендером** — для новых колод сначала готовится текстовая/markdown структура на утверждение, и только потом — вёрстка.
5. **Точечные правки** — короткие сообщения со ссылкой на конкретный номер слайда и точную формулировку — стандартный формат запросов.

---

*Документ сформирован на основе истории работы в проекте по состоянию на август 2026.*
