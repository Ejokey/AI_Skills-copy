Python SDK
¶
AsyncAgenticOSClient
и sync
AgenticOSClient
против REST API. ADR-0056 + TD-048 resilience layer.
Установка
¶
pip
install
-e
".[client]"
# httpx
Async client
¶
from
agenticos.client
import
AsyncAgenticOSClient
async
with
AsyncAgenticOSClient
(
base_url
=
"http://localhost:8000"
,
auth_token
=
"<delegation_jwt>"
,
)
as
client
:
health
=
await
client
.
health
()
agents
=
await
client
.
list_agents
()
session
=
await
client
.
create_session
(
agents
[
0
]
.
agent_id
)
response
=
await
client
.
post_message
(
session
.
session_id
,
"Привет!"
)
print
(
response
.
content
)
async
for
event
in
client
.
stream_events
(
session
.
session_id
):
print
(
event
.
type
,
event
.
sequence
)
Sync client
¶
from
agenticos.client
import
AgenticOSClient
with
AgenticOSClient
(
base_url
=
"http://localhost:8000"
,
auth_token
=
"..."
)
as
client
:
health
=
client
.
health
()
agents
=
client
.
list_agents
()
# ...
Retry и circuit breaker (TD-048)
¶
from
agenticos.client
import
(
AsyncAgenticOSClient
,
RetryPolicy
,
CircuitBreaker
,
AsyncRetryingTransport
,
)
retry
=
RetryPolicy
(
max_attempts
=
5
,
initial_backoff_seconds
=
0.5
,
max_backoff_seconds
=
10.0
,
jitter
=
True
,
retry_status_codes
=
{
408
,
429
,
500
,
502
,
503
,
504
},
)
breaker
=
CircuitBreaker
(
failure_threshold
=
5
,
success_threshold
=
2
,
cooldown_seconds
=
30.0
,
)
client
=
AsyncAgenticOSClient
(
base_url
=
"http://localhost:8000"
,
retry_policy
=
retry
,
circuit_breaker
=
breaker
,
)
Retry-After
header распознаётся (seconds и HTTP-date варианты). Breaker state-machine:
closed → open → half-open
с cooldown.
Production smoke
¶
Файл
scripts/production_smoke.py
— 10 acceptance сценариев через SDK:
async
with
AsyncAgenticOSClient
(
base_url
=
"http://localhost:8000"
,
auth_token
=...
)
as
client
:
# 1. Health
assert
await
client
.
health
()
==
"ok"
# 2. Multi-turn conversation
session
=
await
client
.
create_session
(
agent_id
)
r1
=
await
client
.
post_message
(
session
.
session_id
,
"Hello"
)
r2
=
await
client
.
post_message
(
session
.
session_id
,
"Continue please"
)
# 3. Memory persistence across container restart
# ... container restart ...
r3
=
await
client
.
post_message
(
session
.
session_id
,
"Remember context?"
)
# 4-10. ...
Зафиксированный baseline: 10/10 PASS, ~14 секунд wall-clock, $0.013690 (Haiku 4.5).
Дальше
¶
ADR-0056
.
HTTP API
— server side.