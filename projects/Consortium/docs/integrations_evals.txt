Eval pipeline
¶
Native
EvalRunner
+
LLMJudge
и adapter'ы к Inspect AI, Promptfoo, OdysseyBench, BitGN ECOM. Pass^k multi-seed methodology.
Native EvalRunner
¶
from
agenticos
import
EvalRunner
,
EvalTask
,
LLMJudge
task
=
EvalTask
(
task_id
=
"task-1"
,
user_prompt
=
"What is 2+2?"
,
judge_criteria
=
"Answer should be 4 or contain it."
,
expected_outcome
=
"4"
,
)
judge
=
LLMJudge
(
llm
=
q_llm
,
model_id
=
"anthropic/claude-haiku-4.5"
,
)
runner
=
EvalRunner
(
judge
=
judge
,
harness_factory
=
lambda
:
Harness
(
...
),
)
result
=
await
runner
.
run
([
task
])
LLMJudge
использует
QuarantinedLLM
— judging выводы изолированы от main agent's tools.
Pass^k multi-seed (ADR-0092)
¶
from
agenticos
import
MultiSeedEvalRunner
,
aggregate_trials
,
summarize_multi_seed
runner
=
MultiSeedEvalRunner
(
base_runner
=
eval_runner
,
seeds
=
[
1
,
2
,
3
,
4
],
judge
=
judge
,
)
result
=
await
runner
.
run
([
task
])
summary
=
summarize_multi_seed
(
result
)
# {'pass_at_1': 0.85, 'pass_at_k': 0.62, 'stddev': 0.04, ...}
Inspect AI adapter (ADR-0031)
¶
Bidirectional. Конверсия
EvalTask ↔ Sample
:
pip
install
-e
".[evals-inspect]"
# inspect-ai
from
agenticos.evals
import
(
build_inspect_task
,
harness_solver
,
judge_scorer
,
from_inspect_dataset
,
from_inspect_sample
,
)
from
inspect_ai
import
eval
# AgenticOS → Inspect: прогон через native inspect-ai cli
inspect_task
=
build_inspect_task
(
tasks
=
[
task
],
solver
=
harness_solver
(
harness_factory
=...
),
scorer
=
judge_scorer
(
judge
=
judge
),
)
result
=
eval
(
tasks
=
[
inspect_task
],
model
=
"anthropic/claude-haiku-4.5"
)
# Inspect → AgenticOS: SWE-bench / GAIA / MMLU как EvalTask
swe_tasks
=
from_inspect_dataset
(
swe_bench_dataset
)
agenticos_runner
.
run
(
swe_tasks
)
Promptfoo adapter (ADR-0043)
¶
pip
install
-e
".[evals-promptfoo]"
# pyyaml
from
agenticos.evals
import
(
load_promptfoo_yaml
,
from_promptfoo_config
,
to_promptfoo_eval_output
,
)
config
=
load_promptfoo_yaml
(
"promptfooconfig.yaml"
)
agenticos_tasks
=
from_promptfoo_config
(
config
)
result
=
await
runner
.
run
(
agenticos_tasks
)
# Экспорт в Promptfoo v2 JSON для их web UI
promptfoo_output
=
to_promptfoo_eval_output
(
result
)
OdysseyBench (ADR-0067)
¶
5 категорий: Word, Excel, PDF, Calendar, Email.
from
agenticos.evals.odyssey
import
(
load_odyssey_task
,
OdysseyEvalRunner
,
EVALUATOR_REGISTRY
,
evaluate_exact_match
,
evaluate_contain
,
)
task
=
load_odyssey_task
(
"tests/fixtures/odyssey/inputs/task_excel_revenue.json"
)
runner
=
OdysseyEvalRunner
(
harness_factory
=...
,
evaluator_registry
=
EVALUATOR_REGISTRY
)
result
=
await
runner
.
run
([
task
])
Wave 2 baseline (2026-05-20): ⅘ PASS, 80%, $0.074, 31 секунда.
BitGN ECOM (ADR-0049)
¶
10-tool wrapper над BitGN ECOM VM RPC API.
from
agenticos.evals.bitgn_ecom
import
(
bitgn_tools_for_vm
,
run_benchmark
,
BITGN_SYSTEM_PROMPT
,
DEFAULT_BENCHMARK_ID
,
)
# Регистрация 10 tools (read, write, list, find, ...) против vm_url
tools_for_run
=
bitgn_tools_for_vm
(
vm_url
=
"..."
,
api_key
=...
)
for
t
,
exe
in
tools_for_run
:
tool_manager
.
register
(
t
,
exe
)
report
=
await
run_benchmark
(
benchmark_id
=
DEFAULT_BENCHMARK_ID
,
harness_factory
=
lambda
:
Harness
(
...
,
tools
=
tool_manager
),
trials
=
3
,
)
CI integration
¶
Eval gates запускаются в CI:
# .github/workflows/eval.yml
-
run
:
python scripts/run_odyssey_eval.py --tasks task_excel_revenue,task_email_compose --output baseline.json
-
uses
:
actions/upload-artifact@v4
with
:
{
name
:
eval-baseline
,
path
:
baseline.json
}
Дальше
¶
ADR-0025
.
ADR-0031
.
ADR-0067
.
ADR-0092
.
Self-improvement loop
.